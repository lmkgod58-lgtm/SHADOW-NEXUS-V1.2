document.addEventListener("DOMContentLoaded",async()=>{const box=document.getElementById("services"),services=SHADOW_CONFIG.SERVICES;for(const [name,url] of Object.entries(services)){if(!url)continue;const el=document.createElement("div");el.className="panel";el.innerHTML=`<h2>${name}</h2><p>Checking...</p>`;box.appendChild(el);const t=performance.now();try{const r=await fetch(url.replace(/\/$/,"")+"/health");el.querySelector("p").textContent=`ONLINE • ${Math.round(performance.now()-t)} ms • HTTP ${r.status}`}catch(e){el.querySelector("p").textContent="OFFLINE"}}});