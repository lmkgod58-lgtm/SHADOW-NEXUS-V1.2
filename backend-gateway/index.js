require("dotenv").config();const express=require("express"),cors=require("cors"),app=express();app.use(cors());app.use(express.json());const PORT=process.env.PORT||10000;app.get("/health",(req,res)=>res.json({status:"ok",service:"gateway"}));app.get("/",(req,res)=>res.json({service:"shadow-nexus-gateway",status:"running"}));app.listen(PORT,()=>console.log("Shadow Nexus gateway backend listening on "+PORT));