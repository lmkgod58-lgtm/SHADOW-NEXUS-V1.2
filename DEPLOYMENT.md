# Deployment

## GitHub Pages
Use branch `main` and folder `/ (root)`.

## Render
Deploy each `backend-*` directory as a separate Node service.
Set provider/API secrets as Render environment variables.

## Frontend URLs
After deployment, put only public backend URLs into `js/config.js`.
